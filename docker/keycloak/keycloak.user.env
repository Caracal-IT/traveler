# Keycloak admin credentials for local/dev
# This file is checked into VCS on purpose so the filename is stable.
# Do NOT commit real secrets. Replace values locally before running docker compose.

# Set your admin credentials. Keycloak requires both to start.
# Leave blank values are checked in by default; you MUST change them locally before starting.
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=Admin#1!
